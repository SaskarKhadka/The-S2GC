function SCIncrementColors() {
  activeBoxOn("SC-box");
  activeSignalOn("increment-arrow");
  activeSignalOn("increment-text");
}

function SCClearColors() {
  activeBoxOn("SC-box");
  activeSignalOn("clear-arrow");
  activeSignalOn("clear-text");
}
